{
  "name": "altcha-server",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vercel dev",
    "build": "vercel build",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "altcha-lib": "^0.4.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0"
  }
}
